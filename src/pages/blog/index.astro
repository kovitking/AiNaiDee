---
// blog.ainaidee.com is the real blog now (Ghost serves it directly, see
// CLAUDE.md "Blog" section) — this route used to render its own copy of the
// post list via the Ghost Content API, which left two indexable, self-
// canonicalizing URLs for the same content with no link between them.
export const prerender = false

return Astro.redirect("https://blog.ainaidee.com/", 301)
---
