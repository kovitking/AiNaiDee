---
// blog.ainaidee.com is the real blog now — see src/pages/blog/index.astro
// and CLAUDE.md's "Blog" section. Ghost serves one copy per post, not a
// separate English one, so this redirects to the same URL as the Thai route.
export const prerender = false

const { slug } = Astro.params
return Astro.redirect(`https://blog.ainaidee.com/${slug}/`, 301)
---
